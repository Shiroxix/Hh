# Brawl Stars Proxy (seguro)

## Rodar
```bash
npm i
BRAWLSTARS_TOKEN="SEU_TOKEN" node index.js
```

Endpoints:
- GET /api/health
- GET /api/player/:tag
- GET /api/player/:tag/battlelog
- GET /api/club/:tag
- GET /api/events
- GET /api/brawlers
- GET /api/rankings/:scope/:type?limit=50
